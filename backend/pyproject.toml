[project]
name = "alpharadar-backend"
version = "0.1.0"
description = "FastAPI backend and LangGraph worker scaffold for AlphaRadar."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.15.0",
    "fastapi>=0.115.0",
    "langgraph>=0.6.0",
    "psycopg[binary]>=3.2.0",
    "pydantic-settings>=2.8.0",
    "redis>=5.2.0",
    "sqlalchemy>=2.0.0",
    "uvicorn[standard]>=0.34.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.0",
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.uv]
default-groups = ["dev"]
